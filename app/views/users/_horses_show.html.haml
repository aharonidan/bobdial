%table.table.is-full-width
	%tbody
		%tr
			%td
				%label.label Black Horse
			%td= current_user.black_horse.name.titleize

		%tr
			%td
				%label.label Grey Horse
			%td= current_user.grey_horse.name.titleize

		%tr
			%td
				%label.label Champion
			%td= current_user.champion.name.titleize

		%tr
			%td
				%label.label Top Scorer
			%td= current_user.top_scorer

		%tr
			%td
				%label.label After army trip
			%td= current_user.after_army_trip.titleize